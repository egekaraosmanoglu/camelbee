<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2023 Rahmi Ege Karaosmanoglu

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.camelbee</groupId>
        <artifactId>dependencies</artifactId>
        <version>4.0.1</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <name>CamelBee Quarkus Dependencies</name>
    <artifactId>camelbee-quarkus-dependencies</artifactId>
    <packaging>pom</packaging>


    <properties>

        <quarkus.platform.version>3.39.1</quarkus.platform.version>
        <camel-quarkus.platform.version>${quarkus.platform.version}</camel-quarkus.platform.version>
        <quarkiverse-artemis.version>3.0.0</quarkiverse-artemis.version>
        <assertj-core.version>3.27.7</assertj-core.version>
        <mockito-junit-jupiter.version>5.23.0</mockito-junit-jupiter.version>
        <quarkus-logging-json.version>3.5.0</quarkus-logging-json.version>
        <quarkiverse-artemis-jms.version>3.14.3</quarkiverse-artemis-jms.version>
        <camel-quarkus-rabbitmq.version>2.16.0</camel-quarkus-rabbitmq.version>
        <quarkus.platform.group-id>io.quarkus.platform</quarkus.platform.group-id>
        <quarkus.platform.artifact-id>quarkus-bom</quarkus.platform.artifact-id>
        <camel-quarkus.platform.group-id>${quarkus.platform.group-id}</camel-quarkus.platform.group-id>
        <camel-quarkus.platform.artifact-id>quarkus-camel-bom</camel-quarkus.platform.artifact-id>
        <org.awaitility.awaitility>4.3.0</org.awaitility.awaitility>
        <org.jetbrains.kotlin.kotlin-reflect.version>2.4.20</org.jetbrains.kotlin.kotlin-reflect.version>
        <com.google.crypto.tink.version>1.23.0</com.google.crypto.tink.version>
        <!-- MCP support for generated microservices (declared conditionally in the archetype). -->
        <quarkus-mcp-server.version>1.13.2</quarkus-mcp-server.version>
        <mcp-sdk.version>2.0.1</mcp-sdk.version>
        <!-- langchain4j BOM for the LANGCHAIN4JAGENT / LANGCHAIN4JEMBEDDING backends (declared
             conditionally in the archetype). Verified against the actual 1.15.1 jars (javap on
             AnthropicChatModel$Builder / MistralAiChatModel$Builder etc.) for base URLs, endpoint
             paths, auth headers, and response shapes — don't bump without re-verifying provider
             wiring in the archetype. Kept in sync with the SpringBoot side. -->
        <langchain4j.version>1.19.0</langchain4j.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>${quarkus.platform.group-id}</groupId>
                <artifactId>${quarkus.platform.artifact-id}</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>${camel-quarkus.platform.group-id}</groupId>
                <artifactId>${camel-quarkus.platform.artifact-id}</artifactId>
                <version>${camel-quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.quarkiverse.artemis</groupId>
                <artifactId>quarkus-artemis-jms</artifactId>
                <version>${quarkiverse-artemis-jms.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.camel.quarkus</groupId>
                <artifactId>camel-quarkus-rabbitmq</artifactId>
                <version>${camel-quarkus-rabbitmq.version}</version>
            </dependency>

            <dependency>
                <groupId>org.awaitility</groupId>
                <artifactId>awaitility</artifactId>
                <version>${org.awaitility.awaitility}</version>
            </dependency>

            <!-- Required for native compilation of jms component -->
            <dependency>
              <groupId>org.jetbrains.kotlin</groupId>
              <artifactId>kotlin-reflect</artifactId>
              <version>${org.jetbrains.kotlin.kotlin-reflect.version}</version>
            </dependency>

            <!-- Required for native compilation of jwt-->
            <dependency>
              <groupId>com.google.crypto.tink</groupId>
              <artifactId>tink</artifactId>
              <version>${com.google.crypto.tink.version}</version>
            </dependency>

            <!-- MCP support for generated microservices (declared conditionally in the archetype). -->
            <dependency>
              <groupId>io.quarkiverse.mcp</groupId>
              <artifactId>quarkus-mcp-server-http</artifactId>
              <version>${quarkus-mcp-server.version}</version>
            </dependency>
            <dependency>
              <groupId>io.quarkiverse.mcp</groupId>
              <artifactId>quarkus-mcp-server-test</artifactId>
              <version>${quarkus-mcp-server.version}</version>
            </dependency>
            <dependency>
              <groupId>io.modelcontextprotocol.sdk</groupId>
              <artifactId>mcp</artifactId>
              <version>${mcp-sdk.version}</version>
            </dependency>
            <dependency>
              <groupId>com.networknt</groupId>
              <artifactId>json-schema-validator</artifactId>
              <version>${json-schema-validator.version}</version>
            </dependency>

            <!-- langchain4j BOM for the LANGCHAIN4JAGENT / LANGCHAIN4JEMBEDDING backends. Pins every
                 dev.langchain4j:* artifact (provider models, embedding stores) to one mutually
                 compatible set. Imported after the Quarkus platform BOMs so those win on overlap. -->
            <dependency>
              <groupId>dev.langchain4j</groupId>
              <artifactId>langchain4j-bom</artifactId>
              <version>${langchain4j.version}</version>
              <type>pom</type>
              <scope>import</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

</project>
