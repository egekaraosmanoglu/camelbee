<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<FindBugsFilter>

  <Match>
    <Bug pattern="NP_NULL_ON_SOME_PATH_FROM_RETURN_VALUE"/>
  </Match>

  <Match>
    <Bug pattern="RV_RETURN_VALUE_IGNORED_NO_SIDE_EFFECT"/>
  </Match>

  <Match>
    <Bug pattern="EI_EXPOSE_REP"/>
  </Match>

  <Match>
    <Bug pattern="EI_EXPOSE_REP2"/>
  </Match>

  <Match>
    <Class name="~.*\.*Impl"/>
  </Match>

  <!--
    AuthService derives its signing key in the constructor, and that derivation rethrows a
    NoSuchAlgorithmException for SHA-256 - which no conformant JVM can raise. SpotBugs flags the
    throw because a partially constructed object is exposed to a finalizer attack, whose standard
    remedy is to make the class final. That is not available here: the class is produced as an
    @ApplicationScoped CDI bean on Quarkus, so ARC has to generate a client proxy subclass of it.
    The attack it warns about also needs a finalizer to exist; this class declares none, is not
    Serializable, and holds no resource a subclass could resurrect. Narrowed to the one class so the
    check keeps running everywhere else.
  -->
  <Match>
    <Class name="~.*\.security\.AuthService"/>
    <Bug pattern="CT_CONSTRUCTOR_THROW"/>
  </Match>

  <!--
    ExchangeUtils holds the process-wide Masker in a static field so every tracer and the topology
    redact through the same configuration, rather than each carrying its own copy that could drift
    out of sync after configureMasking() runs. SpotBugs flags both the write and the getter because
    Masker isn't a JDK type it recognises as immutable, but it is immutable in practice: `final
    class`, every field `private final`, built once in the constructor and never mutated or exposed
    as a live collection afterward. Narrowed to this class so the check keeps running everywhere else.
  -->
  <Match>
    <Class name="~.*\.utils\.ExchangeUtils"/>
    <Bug pattern="EI_EXPOSE_STATIC_REP2"/>
  </Match>

  <Match>
    <Class name="~.*\.utils\.ExchangeUtils"/>
    <Bug pattern="MS_EXPOSE_REP"/>
  </Match>

  <!-- === Generated Code Exclusions === -->

  <!-- Exclude all generated sources by source file path -->
  <Match>
    <Source name="~.*generated-sources.*"/>
  </Match>

  <!-- Google Protobuf classes -->
  <Match>
    <Package name="~com\.google\.protobuf.*"/>
  </Match>

  <!-- Google Type classes -->
  <Match>
    <Package name="~com\.google\.type.*"/>
  </Match>

  <!-- Any package containing 'proto' -->
  <Match>
    <Package name="~.*\.grpc.*"/>
  </Match>

  <!-- Any package containing 'proto' -->
  <Match>
    <Package name="~.*\.proto.*"/>
  </Match>

  <!-- Any package containing 'avro' -->
  <Match>
    <Package name="~.*\.avro.*"/>
  </Match>

  <!-- Any package containing 'generated' -->
  <Match>
    <Package name="~.*\.generated.*"/>
  </Match>

  <!-- OpenAPI generated code -->
  <Match>
    <Package name="~.*\.openapi.*"/>
  </Match>

  <!-- Exclude classes that end with common generated suffixes -->
  <Match>
    <Class name="~.*OuterClass.*"/>
  </Match>

  <Match>
    <Class name="~.*Proto.*"/>
  </Match>

  <!-- Exclude specific SpotBugs rules for generated code -->
  <Match>
    <Bug pattern="MS_EXPOSE_REP"/>
    <Class name="~.*\.proto\..*"/>
  </Match>

  <Match>
    <Bug pattern="MS_EXPOSE_REP"/>
    <Class name="~.*\.grpc\..*"/>
  </Match>

  <Match>
    <Bug pattern="MS_EXPOSE_REP"/>
    <Class name="~.*\.avro\..*"/>
  </Match>

  <Match>
    <Bug pattern="MS_EXPOSE_REP"/>
    <Class name="~.*Proto.*"/>
  </Match>

  <Match>
    <Bug pattern="SING_SINGLETON_IMPLEMENTS_SERIALIZABLE"/>
    <Class name="~.*\.proto\..*"/>
  </Match>

  <Match>
    <Bug pattern="SING_SINGLETON_IMPLEMENTS_SERIALIZABLE"/>
    <Class name="~.*\.grpc\..*"/>
  </Match>

  <Match>
    <Bug pattern="SING_SINGLETON_IMPLEMENTS_SERIALIZABLE"/>
    <Class name="~.*\.avro\..*"/>
  </Match>

  <Match>
    <Bug pattern="SING_SINGLETON_IMPLEMENTS_SERIALIZABLE"/>
    <Class name="~.*Proto.*"/>
  </Match>

  <!-- Exclude getDecoder() method issues in Avro classes -->
  <Match>
    <Bug pattern="MS_EXPOSE_REP"/>
    <Method name="getDecoder"/>
  </Match>

  <!-- Exclude getDefaultInstance() method issues -->
  <Match>
    <Bug pattern="MS_EXPOSE_REP"/>
    <Method name="getDefaultInstance"/>
  </Match>

  <!-- Exclude getDescriptor() method issues -->
  <Match>
    <Bug pattern="MS_EXPOSE_REP"/>
    <Method name="getDescriptor"/>
  </Match>

</FindBugsFilter>