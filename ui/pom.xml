<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.camelbee</groupId>
    <artifactId>camelbee</artifactId>
    <version>4.0.1</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>camelbee-ui</artifactId>
  <packaging>pom</packaging>
  <name>CamelBee UI</name>
  <description>CamelBee embedded React UI for debugger and metrics</description>

  <properties>
    <node.version>v22.23.1</node.version>
    <npm.version>10.9.0</npm.version>
    <frontend-maven-plugin.version>2.0.2</frontend-maven-plugin.version>
    <!--
      frontend-maven-plugin's npm goal only knows about its own skip.npm flag, so the vitest run
      below would otherwise ignore -DskipTests. Declaring a default here lets -DskipTests skip it,
      which is what ui/e2e's rebuild step expects.
    -->
    <skipTests>false</skipTests>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>com.github.eirslett</groupId>
        <artifactId>frontend-maven-plugin</artifactId>
        <version>${frontend-maven-plugin.version}</version>
        <configuration>
          <workingDirectory>${project.basedir}</workingDirectory>
        </configuration>
        <executions>
          <execution>
            <id>install-node-and-npm</id>
            <goals>
              <goal>install-node-and-npm</goal>
            </goals>
            <configuration>
              <nodeVersion>${node.version}</nodeVersion>
              <npmVersion>${npm.version}</npmVersion>
            </configuration>
          </execution>
          <execution>
            <id>npm-install</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>install</arguments>
            </configuration>
          </execution>
          <execution>
            <id>npm-test</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run test:coverage</arguments>
              <skip>${skipTests}</skip>
            </configuration>
          </execution>
          <execution>
            <id>npm-build</id>
            <goals>
              <goal>npm</goal>
            </goals>
            <configuration>
              <arguments>run build</arguments>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
