<?xml version="1.0" encoding="UTF-8"?>
<!--
  Copyright 2023 Rahmi Ege Karaosmanoglu

  Licensed under the Apache License, Version 2.0 (the "License");
  you may not use this file except in compliance with the License.
  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.camelbee</groupId>
        <artifactId>camelbee-standalone-starter</artifactId>
        <version>4.0.1</version>
        <relativePath>../../starters/camelbee-standalone-starter/pom.xml</relativePath>
    </parent>

    <artifactId>allcomponent-standalone-sample</artifactId>
    <version>4.0.1</version>
    <packaging>jar</packaging>

    <name>allcomponent-standalone-sample</name>
    <description>CamelBee standalone (camel-main) example with an EIP-rich, infra-free topology</description>

    <dependencies>
        <!-- infra-free Camel components used by the demo routes -->
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-timer</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-direct</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-log</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-mock</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-bean</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-file</artifactId>
        </dependency>
        <!-- in-memory queue: gives the topology a pollable, non-direct internal endpoint
             (poll/pollEnrich targets) without needing a real broker -->
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-seda</artifactId>
        </dependency>
        <!-- http producer used to call this application's own REST port, so the topology
             contains a genuinely remote hop while staying infrastructure-free -->
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-http</artifactId>
        </dependency>

        <!-- Logging backend for the standalone app. Logback (not slf4j-simple) is used because it
             provides a working MDC adapter, which CamelBee's structured logging relies on to surface
             requestId/transactionId/routeId/endpoint via the %X pattern in logback.xml. -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback-classic.version}</version>
        </dependency>

        <!-- Integration tests. They boot this application in-process and drive the CamelBee HTTP
             API, so no server, mocking or container framework is needed beyond JUnit + AssertJ. -->
        <!-- brings JUnit 5, version-managed by camel-bom (same approach as standalone-core) -->
        <dependency>
            <groupId>org.apache.camel</groupId>
            <artifactId>camel-test-junit5</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <version>${assertj-core.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <mainClass>io.camelbee.standalone.example.Application</mainClass>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
